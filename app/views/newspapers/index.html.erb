<h1>Newspapers</h1>
<ul>
	<% @newspapers.each do |newspaper| %>
		<li>
			<%= link_to newspaper.title, newspaper_path(newspaper) %>
			--
			<small>
				<%= newspaper.editor %>
			</small>
		</li>
	<% end %>
</ul>
<%= link_to "Add", new_newspaper_path %>